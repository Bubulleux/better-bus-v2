import 'package:flutter/material.dart';

class ConnectionNeeded extends StatelessWidget {
  const ConnectionNeeded({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
